#ifndef PREPAREFORGCP_INCLUDED
#define PREPAREFORGCP_INCLUDED
#include <vector>
using namespace std;

void preStepsGCP(int &numNodes, int &numEdges,vector<int> &numNodesIndepSet, int &numPrecolorsChosen, int &numColorsChosen, int &many);

#endif
